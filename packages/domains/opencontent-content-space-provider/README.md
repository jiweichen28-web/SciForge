# OpenContent Content Space Provider

Maps the token-free OpenContent Connector facade into the provider-neutral ContentSpaceProvider contract. It owns no credentials, HTTP, Project state, Workspace state, or Shared Documents behavior.
